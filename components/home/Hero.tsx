/**
 * Hero — plain solid section. All foreground content and all background
 * effects (galaxy particles, gradients, gold glow, film grain, vignette)
 * have been removed.
 */
export function Hero() {
  return (
    <section className="relative min-h-[90vh] overflow-hidden bg-ink" aria-hidden />
  );
}
